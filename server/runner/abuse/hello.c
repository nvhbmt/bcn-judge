/* Ca số 0 (design.md §3.2): compile + run end-to-end phải xanh.
   Lớp lỗi "mọi submission thành CE" bắt buộc bị bắt ở P0. */
#include <stdio.h>

int main(void) {
    printf("hello\n");
    return 0;
}
